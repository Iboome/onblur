#  Spring Security
# 指定默认的用户名，默认为user.
security.user.name=ronger
# 默认的用户密码.
security.user.password=ronger
# 默认用户的授权角色.
security.user.role=USER
# 是否对所有请求开启SSL，默认为false.
security.require-ssl=false
# 是否开启cross-site request forgery校验，默认为false.
security.enable-csrf=false
# 是否开启基本的鉴权，默认为true
security.basic.enabled=true
# HTTP basic realm 的名字，默认为Spring
security.basic.realm=Spring
# 需要鉴权的path，多个的话以逗号分隔，默认为[/**]
security.basic.path=/user
# 要使用的权限控制模式.
security.basic.authorize-mode=authenticated
# Security filter chain的order，默认为0
security.filter-order=0
#是否开启cross-site scripting (XSS) 保护，默认为false.
security.headers.xss=false
# 是否开启http头部的cache控制，默认为false.
security.headers.cache=false
# 是否开启X-Frame-Options头部，默认为false.
security.headers.frame=false
# 是否开启X-Content-Type-Options头部，默认为false.
security.headers.content-type=false
# 指定HTTP Strict Transport Security (HSTS)模式(none, domain, all).
security.headers.hsts=all
#指定Session的创建策略(always, never, if_required, stateless).
security.sessions=stateless
#  指定不鉴权的路径，多个的话以逗号分隔.
security.ignored= /,/barrage,/article

spring.profiles.active=dev
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=LEGACYHTML5
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.content-type=text/html
spring.thymeleaf.cache=false

spring.datasource.url=jdbc:mysql://127.0.0.1/onblur?characterEncoding=UTF-8&autoReconnect=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=d9d2j9w2
spring.datasource.driverClassName=com.mysql.jdbc.Driver

spring.datasource.max-active=5
spring.datasource.max-idle=2
spring.datasource.min-idle=1
spring.datasource.initial-size=1
spring.datasource.initialize=false
spring.datasource.test-on-borrow=false
spring.datasource.test-on-return=false
spring.datasource.test-while-idle=true
#spring.datasource.max-wait-millis=30000
spring.datasource.validation-query=SELECT 1
spring.datasource.time-between-eviction-runs-millis=20000
spring.datasource.min-evictable-idle-time-millis=28700

# Specify the DBMS
spring.jpa.database = MYSQL
# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect